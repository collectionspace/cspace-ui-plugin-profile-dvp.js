import { defineMessages } from 'react-intl';

export default {
  inputTable: defineMessages({
    interviewStatusTable: {
      id: 'inputTable.intake.interviewStatusTable',
      defaultMessage: 'Interview status',
    },
  }),
};
